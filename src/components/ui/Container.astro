---
interface Props {
  as?: string;
  class?: string;
}
const { as: Tag = 'div', class: className = '' } = Astro.props;
---
<Tag class:list={["mx-auto w-full max-w-container px-6 md:px-8", className]}>
  <slot />
</Tag>
